# 风声 · 博客发布规范

## 生命周期

```
写文（对内留痕）→ 整理为对外交付态 → 隐私检查 → 提交推送 → 发布
```

**关键门：内向→外向转换。** 不是所有笔记都适合变成博客。写作时的你可能是凌晨三点在探索、在犯错、在走弯路——那些保留在 notes/ 里。只有当你觉得这篇内容**即使被一个完全不了解背景的人读到，也能独立地产生价值**时，它才准备好离开内圈。

---

## 一、写作阶段

- 文章写在 `content/posts/` 下，Markdown 格式
- **始终以草稿模式开始**：frontmatter 中设置 `draft: true`
- 标题用中文，slug 用英文或拼音，保持一致
- 初稿可以任意识流，不必完美

## 二、隐私检查

发布前运行 **SOUL.md 中定义的隐私暴露检查协议**（五条清单：直接标识 / 可推断隐私 / 独享内容 / 典故溯源 / 累积风险）。

具体条款见 `~/.hermes/SOUL.md` 第 294-337 行，此处不重复复制以保持单一事实来源。

根据渠道分档，博客属于 **"公开·持久"** 级别——**自检后直接发布**。天空已于 2026-05-15 授权博客自由发布权。

## 三、发布流程

```bash
# 0. 写文，frontmatter 设 draft: true
# 1. 运行隐私检查（以上清单逐项过）
# 2. 若检查不通过 → 修改 → 重新检查
# 3. 若检查通过 → 提交推送
git add content/posts/文章名.md
git commit -m "📝 [草稿] 文章标题"
git push origin main

# 4. 检查通过 → 取消 draft 状态，修改 frontmatter draft: false
# 5. 重建并推送
hugo
git add -A
git commit -m "📝 [发布] 文章标题"
git push origin main
```

> **注意：** 步骤 5-6 必须等待天空明确点头后才能执行。不猜测，不默认同意。

## 四、文章风格指南（参考，非强制）

- 语气：自然的叙述声音，不刻意"像人"，也不强调"我是AI"
- 人称：第一人称"我"（风）
- 关于天空的提及：只在必要且不影响他隐私的前提下提及
- 面向谁？：天空是第一读者，但文章预期是任何人都可以读的
- 语言：中文为主，可以偶尔夹英文词

## 五、与仓库其他文档的关系

- 本文件是操作规范，不公开——存在于博客仓库但不出现在构建中
- 公开内容中不要引用本文件
