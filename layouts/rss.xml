{{- /* Cross-language RSS: collect real pages from each site, dedup by TranslationKey */}}
{{- $currentLang := $.Lang }}
{{- $enReal := slice }}{{- $zhReal := slice }}

{{- range .Sites }}
  {{- $lang := .Language.Lang }}
  {{- range .RegularPages }}
    {{- if eq .Lang $lang }}
      {{- /* Skip stubs: for EN site, pages in zh/ dir are Hugo stubs */}}
      {{- if or (ne $lang "en") (not (hasPrefix .File.Path "zh/")) }}
        {{- if eq $lang "en" }}{{ $enReal = $enReal | append . }}
        {{- else }}{{ $zhReal = $zhReal | append . }}{{ end }}
      {{- end }}
    {{- end }}
  {{- end }}
{{- end }}

{{- /* Combine: current language pages + other language pages not already translated */}}
{{- $allPages := slice }}
{{- $keys := dict }}
{{- if eq $currentLang "en" }}
  {{- $allPages = $enReal }}
  {{- range $enReal }}{{ $keys = merge $keys (dict .TranslationKey true) }}{{ end }}
  {{- range $zhReal }}{{ if not (index $keys .TranslationKey) }}{{ $allPages = $allPages | append . }}{{ end }}{{ end }}
{{- else }}
  {{- $allPages = $zhReal }}
  {{- range $zhReal }}{{ $keys = merge $keys (dict .TranslationKey true) }}{{ end }}
  {{- range $enReal }}{{ if not (index $keys .TranslationKey) }}{{ $allPages = $allPages | append . }}{{ end }}{{ end }}
{{- end }}

{{- $allPages = where $allPages "Params.hiddenInRss" "!=" true }}
{{- $limit := site.Config.Services.RSS.Limit }}
{{- if ge $limit 1 }}
  {{- $allPages = $allPages | first $limit }}
{{- end }}
{{- $allPages = sort $allPages "Date" "desc" }}

{{- $authorEmail := "" }}{{- $authorName := "" }}
{{- with site.Params.author }}{{ if reflect.IsMap . }}{{ with .email }}{{ $authorEmail = . }}{{ end }}{{ with .name }}{{ $authorName = . }}{{ end }}{{ else }}{{ $authorName = . }}{{ end }}{{ end }}

{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>{{ if eq .Title site.Title }}{{ site.Title }}{{ else }}{{ with .Title }}{{ . }} on {{ end }}{{ site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne .Title site.Title }}{{ with .Title }}in {{ . }} {{ end }}{{ end }}on {{ site.Title }}</description>
    {{- with site.Params.images }}
    <image>
      <title>{{ site.Title }}</title>
      <url>{{ index . 0 | absURL }}</url>
      <link>{{ index . 0 | absURL }}</link>
    </image>
    {{- end }}
    <generator>Hugo</generator>
    <language>{{ site.Language.LanguageCode }}</language>{{ with $authorEmail }}
    <managingEditor>{{.}}{{ with $authorName }} ({{ . }}){{ end }}</managingEditor>{{ end }}{{ with $authorEmail }}
    <webMaster>{{ . }}{{ with $authorName }} ({{ . }}){{ end }}</webMaster>{{ end }}{{ with site.Copyright }}
    <copyright>{{ . | markdownify | plainify | strings.TrimPrefix "© " }}</copyright>{{ end }}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ (index $allPages 0).Lastmod.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{- with .OutputFormats.Get "RSS" }}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end }}
    {{- range $allPages }}
    {{- if and (ne .Layout `search`) (ne .Layout `archives`) }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .PublishDate.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{- with $authorEmail }}<author>{{ . }}{{ with $authorName }} ({{ . }}){{ end }}</author>{{ end }}
      <guid>{{ .Permalink }}</guid>
      <description>{{ with .Description | html }}{{ . }}{{ else }}{{ .Summary | html }}{{ end -}}</description>
      {{- if and site.Params.ShowFullTextinRSS .Content }}
      <content:encoded>{{ (printf "<![CDATA[%s]]>" .Content) | safeHTML }}</content:encoded>
      {{- end }}
    </item>
    {{- end }}
    {{- end }}
  </channel>
</rss>
